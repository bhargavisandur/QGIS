const geocoder = require('./index')({
  provider: 'google',
  httpAdapter: 'fetch'
});

async function run() {
  try {
    const res = await geocoder.geocode('1573 avenue laurier, montreal');
    console.log(res);
  } catch (err) {
    console.log(err);
  }
}

run();
